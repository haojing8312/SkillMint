{
  "name": "skillmint",
  "private": true,
  "scripts": {
    "app": "pnpm --filter runtime tauri dev",
    "runtime": "pnpm --filter runtime tauri dev",
    "build:app": "pnpm --filter runtime tauri build",
    "build:runtime": "pnpm --filter runtime tauri build",
    "sync:codex-skills": "node scripts/sync-codex-skills.mjs"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
